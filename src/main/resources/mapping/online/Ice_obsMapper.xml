<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tongji.enso.mybatisdemo.mapper.online.Ice_obsMapper">

    <select id="findByYear" resultType="Ice_obs">
        select * from ice_obs where year= #{year} order by month
    </select>

</mapper>